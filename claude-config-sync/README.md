# Claude Config Sync

A Claude Code plugin for synchronizing configuration across multiple environments.

## Installation

This plugin is loaded from the local `./claude-config-sync` directory.

## Usage

The plugin provides commands and skills for managing Claude Code configuration synchronization.

## Features

- Configuration sync across environments
- Easy setup and configuration
- Support for multiple Claude Code instances
